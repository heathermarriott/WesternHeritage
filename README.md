# WesternHeritage

A prototype of possible HTML menu system with proto luma videos playing in the background.
Right now the video play is laggy, so I just have placeholder images.

languages.html is my idea to provided a limited number of translation languages 
(not exactly the same as having the avatar speak in other languages, but easier to implement)

ToDo: 
1) Under settings, when user selects Proto size, display all webpages in dimentions that work for proto
2) run video on local machine to reduce lag
3) ~~full language support for html text as shown in languages.html~~
4) check with Project Sponsors before proceeding much further with this model
5) Implement "Ask Questions" webpage with buttons that direct the video that will play based on the list of videos that are available.  Ex. "What are the Rough Riders?", etc.

To test the website:
python3 -m http.server 8000  
then access with access with http://localhost:8000/

Proto Luma Website Dimensions: 2160 x 3840   9x16 aspect ratio
